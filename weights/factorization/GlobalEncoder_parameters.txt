For mid version (middle size, fast, slightly more accurate):

model = GlobalEncoder(
    latent_dim=128, 
    frames_per_atom=21, 
    cnn_hidden=256, 
    transformer_dim=512, 
    num_heads=8, 
    num_layers=4, 
    clap_dim=1024
)

For small version (smaller, faster and slightly less accurate):

model = GlobalEncoder(
    latent_dim=128, 
    frames_per_atom=21, 
    cnn_hidden=256, 
    transformer_dim=256, 
    num_heads=4, 
    num_layers=4, 
    clap_dim=1024
)